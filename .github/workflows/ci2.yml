name: My First CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from My First CI"
      - run: pwd
      - run: ls
      - run: exit 1
      
      - name: Check success
        if: success()
        run: echo "Previous steps were successful"
      - run: bash 02-memory-health-checker/memory_checker.sh  
